# Simple HTML/JS Project

A minimal HTML and JavaScript starter project.

## Files

- `index.html` – page structure and markup
- `styles.css` – basic responsive styling
- `script.js` – simple DOM interaction logic

## Usage

Open `index.html` in your browser to run the project.
